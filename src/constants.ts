export let EXTENSION_NAME = "codeswing";
export let EXTENSION_ID = `codespaces-contrib.${EXTENSION_NAME}`;

export let INPUT_SCHEME = `${EXTENSION_NAME}-input`;

export let SWING_FILE = `${EXTENSION_NAME}.json`;

export let URI_PATTERN = /https?:\/\/(www\.)?[-a-zA-Z0-9@:%._\+~#=]{1,256}\.[a-zA-Z0-9()]{1,6}\b([-a-zA-Z0-9()@:%_\+.~#?&//=]*)/;
